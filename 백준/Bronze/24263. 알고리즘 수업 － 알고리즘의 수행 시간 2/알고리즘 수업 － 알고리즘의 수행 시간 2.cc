#include <stdio.h>
#include <iostream>

using namespace std;

int main()
{
    int n;
    cin >> n;
    //f(n) = n;
    //f(n) = 1 * n^1 이므로 1
    cout << n << "\n";
    cout << 1;
    
}